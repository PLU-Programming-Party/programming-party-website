---
layout: base
title: People
permalink: /people/
templateEngine: njk
---

<div class="people-page">
    <h1>Programming Party Community</h1>
    <p class="intro">Meet the students and alumni who build Programming Party projects</p>

    {% set people = people %}

    <h2>Members</h2>
    <p class="section-subtitle">Current and past contributors</p>
    <div class="people-grid">
        {% for person in people %}
        <div class="person-card">
            <div class="person-avatar">
                {% if person.image %}
                <img src="{{ person.image }}" alt="{{ person.name }}">
                {% else %}
                {{ person.name.charAt(0) }}
                {% endif %}
            </div>
            <h3>{{ person.name }}</h3>
            <p class="person-role">{{ person.role }}</p>
            {% if person.graduationYear %}
            <p class="person-year">Class of {{ person.graduationYear }}</p>
            {% endif %}
            <p class="person-bio">{{ person.bio }}</p>
            {% if person.links.linkedin or person.links.github or person.links.website %}
            <div class="person-links">
                {% if person.links.linkedin %}
                <a href="{{ person.links.linkedin }}" target="_blank" title="LinkedIn" class="link-icon">in</a>
                {% endif %}
                {% if person.links.github %}
                <a href="{{ person.links.github }}" target="_blank" title="GitHub" class="link-icon">gh</a>
                {% endif %}
                {% if person.links.website %}
                {% if person.links.website.startsWith('mailto:') %}
                <a href="{{ person.links.website }}" title="Email" class="link-icon">✉️</a>
                {% else %}
                <a href="{{ person.links.website }}" target="_blank" title="Website" class="link-icon">🌐</a>
                {% endif %}
                {% endif %}
            </div>
            {% endif %}
        </div>
        {% endfor %}
    </div>

    <section class="join-community">
        <h2>Want to Join?</h2>
        <p>Programming Party is always looking for new members! Whether you're interested in games, machine learning, music, memes, or building something completely different, there's a place for you.</p>
        <p><strong>We meet weekly during the school year.</strong> All skill levels welcome!</p>
        <p><a href="mailto:caleyjb@plu.edu" style="color: #ffbe0b; font-weight: 600; text-decoration: underline;">Email Dr. Caley</a> or visit our <a href="https://github.com/PLU-Programming-Party" style="color: #ffbe0b; font-weight: 600; text-decoration: underline;">GitHub organization</a> →</p>
    </section>
</div>